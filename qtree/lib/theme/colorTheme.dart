import 'package:flutter/material.dart';

class ColorTheme {
  static ColorTheme colorTheme = new ColorTheme();

  final Color darkGrey;
  // final Color primaryColor;

  ColorTheme({
    this.darkGrey = const Color(0xFF343541),
    // this.primaryColor = const Color(0xFF),
  });
}
